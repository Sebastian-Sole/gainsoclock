# Implementation Plans

Generated by the improve skill on 2026-06-12 at commit `4500535` (plans
001–005 in the first pass; 006–031 added the same day on operator request to
plan the full backlog). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Every plan stamps `Planned at: 4500535` and carries its own drift check — run
it first; the codebase may have moved.

> **2026-06-14 — ALL 32 PLANS RESOLVED ON `develop`.** The original 30 PRs
> (#45–#74) plus **008** (#76, multi-device merge policy) and **027** (#77,
> investigated → no-op) are all merged into the `develop` integration branch.
> 008 was built on its now-present deps (006/007/009); 027 was rewritten to
> clear its old blocker, then **measured** and closed as not-worth-doing
> (lucide is not a bundle cost — see docs/perf/baseline.md). develop preflight
> green: tsc app + convex, lint 0 errors, vitest 45/45. Final review is the
> single PR **#75 (`develop → main`)**. 31/32 shipped code; 027 is the one
> evidence-based REJECTED. Earlier integration note follows.
>
> **EXECUTION COMPLETE.** 30 of 32 plans executed, reviewed,
> and published as PRs #45–#74 (now merged into develop).
>
> **Wave 1.** 25 plans
> APPROVED after independent re-verification (typechecks, done-criteria
> greps, scope checks, diff review) and opened as PRs #45–#69. Plan 001
> STOPPed correctly on a discovery: `main` fails lint with 9 pre-existing
> errors (rules-of-hooks in the onboarding demo screens) — new plan 032
> fixes that, and 001 re-runs stacked on it. Wave 2 in progress
> (`wf_68238ebe-7da`): 032 → 001, plus 006 (on 004's branch), 018 (on 007's),
> 027 (on 022's), 029 (on 013's). 008 remains deferred until 007/009/006
> merge. Universal review caveat, verified by identical 46-problem lint
> counts across all 25 differently-modified worktrees: no plan introduced
> any lint problem; the baseline failure is main's own (fixed by 032).

## Execution order & status

Recommended order = P1 top-to-bottom, then P2, then P3; within a tier the
table order respects dependencies. Plans without shared files can run in
parallel (see Dependency notes for the exceptions).

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI workflow: typecheck + lint on every PR | P1 | S | 032 | DONE — [PR #71](https://github.com/Sebastian-Sole/gainsoclock/pull/71) (stacked on #70) |
| 032 | Fix the 9 baseline lint errors blocking the CI gate | P1 | S | — | DONE — [PR #70](https://github.com/Sebastian-Sole/gainsoclock/pull/70) |
| 002 | Fix silently no-op RevenueCat logOut (sign-out + deletion) | P1 | S | 001 (soft) | DONE — [PR #45](https://github.com/Sebastian-Sole/gainsoclock/pull/45) |
| 003 | Route all decimal inputs through the locale-aware parser | P1 | S | 001 (soft) | DONE — [PR #46](https://github.com/Sebastian-Sole/gainsoclock/pull/46) |
| 004 | Stop dropping RPE + interval fields in workout-set sync | P1 | S | 001 (soft) | DONE — [PR #47](https://github.com/Sebastian-Sole/gainsoclock/pull/47) |
| 005 | Stop client verification clobbering the subscription state machine | P1 | M | 001 (soft) | DONE — [PR #48](https://github.com/Sebastian-Sole/gainsoclock/pull/48) |
| 007 | Ordered offline sync queue + dead-letter store | P1 | M | 001 (soft) | DONE — [PR #49](https://github.com/Sebastian-Sole/gainsoclock/pull/49) |
| 006 | Wire `listFull` into first-run history hydration | P1 | M | 004 | DONE — [PR #72](https://github.com/Sebastian-Sole/gainsoclock/pull/72) (stacked on #47) |
| 009 | Stop counting yesterday's meals after midnight | P1 | S | — | DONE — [PR #50](https://github.com/Sebastian-Sole/gainsoclock/pull/50) |
| 010 | Fix the daily reminder's suppress-and-restore cycle | P1 | S | — | DONE — [PR #51](https://github.com/Sebastian-Sole/gainsoclock/pull/51) |
| 011 | Include interval sets in stats accumulation | P1 | S | — | DONE — [PR #52](https://github.com/Sebastian-Sole/gainsoclock/pull/52) |
| 015 | HMAC unsubscribe tokens (replace forgeable hash) | P1 | S-M | — | DONE — [PR #56](https://github.com/Sebastian-Sole/gainsoclock/pull/56) |
| 024 | Rebuild the Maestro e2e suite + un-break the canary | P1 | L | 001 (soft) | DONE — [PR #64](https://github.com/Sebastian-Sole/gainsoclock/pull/64) |
| 026 | Reconcile age-gate decision + compliance docs (investigate) | P1 | S-M | — | DONE — [PR #66](https://github.com/Sebastian-Sole/gainsoclock/pull/66) |
| 012 | Contain active-workout per-keystroke re-renders | P2 | M | — | DONE — [PR #53](https://github.com/Sebastian-Sole/gainsoclock/pull/53) |
| 013 | Bound chat history (OpenAI window + capped queries) | P2 | M | — | DONE — [PR #54](https://github.com/Sebastian-Sole/gainsoclock/pull/54) |
| 014 | Bind file-storage operations to an owner | P2 | M | — | DONE — [PR #55](https://github.com/Sebastian-Sole/gainsoclock/pull/55) |
| 016 | Bind the Pro dev-bypass to a specific deployment | P2 | S | — | DONE — [PR #57](https://github.com/Sebastian-Sole/gainsoclock/pull/57) |
| 017 | Consolidate server streak logic + fix PR unit normalization | P2 | M | 025 (soft) | DONE — [PR #58](https://github.com/Sebastian-Sole/gainsoclock/pull/58) |
| 019 | Fix convex/ manifest drift (openai, posthog-node) | P2 | S | — | DONE — [PR #59](https://github.com/Sebastian-Sole/gainsoclock/pull/59) |
| 021 | Real README + .env.example + server env docs | P2 | S | — | DONE — [PR #61](https://github.com/Sebastian-Sole/gainsoclock/pull/61) |
| 029 | Instrument AI-coach context cost + define the trigger | P2 | S | — | DONE — [PR #74](https://github.com/Sebastian-Sole/gainsoclock/pull/74) (stacked on #54) |
| 030 | Ship the "Generate Macros with AI" recipe button | P2 | S-M | — | DONE — [PR #68](https://github.com/Sebastian-Sole/gainsoclock/pull/68) |
| 031 | Finish the HealthKit re-ask eligibility gate | P2 | S | — | DONE — [PR #69](https://github.com/Sebastian-Sole/gainsoclock/pull/69) |
| 008 | Queue-aware hydration merge policy (multi-device correctness) | P2 | L | 007, 006, 009, 025 (soft) | DONE — [PR #76](https://github.com/Sebastian-Sole/gainsoclock/pull/76) (merged into develop) |
| 018 | Type the syncToConvex chokepoint generically | P3 | M | 007 | DONE — [PR #73](https://github.com/Sebastian-Sole/gainsoclock/pull/73) (stacked on #49) |
| 020 | Pin @convex-dev/auth + upgrade procedure doc | P3 | S | 024 (soft) | DONE — [PR #60](https://github.com/Sebastian-Sole/gainsoclock/pull/60) |
| 022 | Make the perf baseline real (cold-start marker + bundle number) | P3 | S-M | — | DONE — [PR #62](https://github.com/Sebastian-Sole/gainsoclock/pull/62) |
| 023 | Validator-derived types drift tripwire + decision (investigate) | P3 | S-M | — | DONE — [PR #63](https://github.com/Sebastian-Sole/gainsoclock/pull/63) |
| 025 | Test-runner decision + characterization baseline (gated) | P3 | M | — | DONE — [PR #65](https://github.com/Sebastian-Sole/gainsoclock/pull/65) |
| 027 | Lucide barrel bundle cost (investigated) | P3 | S-M | 022 (soft) | REJECTED (no code) — [PR #77](https://github.com/Sebastian-Sole/gainsoclock/pull/77): rewritten to unblock the old catalogue-import blocker, then **measured** baseline/Phase-1/Phase-2 all within 2 KB gz → Metro already excludes unused icons; the barrel is not a bundle cost. Both phases reverted; finding recorded in docs/perf/baseline.md. |
| 028 | Data-export design spike (GDPR portability) | P3 | S-M | — | DONE — [PR #67](https://github.com/Sebastian-Sole/gainsoclock/pull/67) |
| 033 | Apple ↔ password account linking (resolve siwa_email_collision) | P1 | M | — | IN PROGRESS — branch `feat/siwa-account-linking` (off develop); user-reported SIWA collision on a 2nd device. Needs Convex deploy + on-device verify (operator). |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first**: it establishes the only automated verification gate; nearly every plan's done criteria lean on a green `npx tsc --noEmit` / `pnpm lint` baseline.
- **004 → 006**: the new-device seed hydrates sets; without 004 it would seed RPE/interval-field-stripped copies.
- **007 → 008 → (009)**: 008 needs 007's `getPendingClientIds` accessor and integrates with 009's date-scoped meal hydrate; do them in that order.
- **007 → 018**: same file (`lib/convex-sync.ts`); 018 rebases on 007's landed shape.
- **002 and 005 both touch `hooks/use-purchases.ts`** (different regions) — fine sequentially, do not run concurrently in one worktree.
- **013 and 029 both touch `convex/chatActions.ts`** — sequential; whichever lands second integrates (each plan says how).
- **025 (characterization tests) softly precedes 008 and 017** — refactors of `lib/` logic and server metrics are safer with the baseline pinned; both plans note the fallback when 025 hasn't run.
- **022 → 027**: the bundle baseline makes 027's before/after honest; 027 has its own measure step as fallback.
- **024 → 020**: the documented auth-upgrade procedure references the rebuilt e2e flows as its gate.
- **026 may spawn new P1 plans**: its deletion-coverage matrix (see New findings below) and the age-gate A/B decision both likely become follow-up plans.

## New findings surfaced while planning 006–031

(Found during the plan-dossier verification pass; recorded here so they aren't lost. Corrections to the original audit are folded in.)

- **NEW-01 (HIGH, investigate in plan 026)**: `convex/user.ts` `deleteAllData` (the Settings "reset data" action) enumerates 14 tables but the schema has 23 — it misses `userProfile`, `userConsents`, `onboardingAha`, `aiSafetyIncidents`, `externalWorkouts`, `healthDailyMetrics`, `weeklyReviews`. Whether the *account-deletion* path (`api.onboarding.deleteAccount`) covers them is unverified; `healthDailyMetrics`/`externalWorkouts` are imported Apple Health data and `docs/privacy-nutrition-label.md` promises synchronous deletion. Plan 026 produces the coverage matrix; a fix plan likely follows.
- **NEW-02 (canary correction)**: the weekly canary's failure is NOT silent (it files `canary`/`on-call` GitHub issues) — but it can never reach Maestro: the workflow installs from `ios/build/...` without any build step producing it. Fixed in plan 024.
- **NEW-03**: `components/paywall/paywall-interstitial.tsx` and `paywall-fallback.tsx` are fully built, testID-rich, and imported nowhere — dead code from the paywall iteration. Cleanup candidate (not planned; trivial deletion once confirmed unwanted).
- **NEW-04**: `convex/onboardingActions.ts` `generateAhaWorkout` has zero client callers (the aha flow was removed in the onboarding pivot) — dead public action surface. Cleanup candidate.
- **NEW-05**: `docs/apple-review-notes.md` also misattributes account deletion to a nonexistent `convex/user.ts deleteAccount` (the client calls `api.onboarding.deleteAccount`) — corrected by plan 026.
- **NEW-06**: `convex/workoutLogs.ts` `bulkUpsert` is insert-only (existing clientIds skipped, never patched) — "upsert" is a misnomer; relevant to any future migration/backfill work (plans 004/006 note the implication).
- Correction: the codebase has 98 testIDs, not 107 as the audit stated.

## Direction findings (maintainer options, planned as spikes/builds)

- **DIR-01 → plan 028** (design spike): data export / GDPR portability.
- **DIR-02 → plan 029** (build): AI-context cost instrumentation + trigger.
- **DIR-03 → plan 030** (build): ship the stubbed AI recipe-macros button.
- **DIR-04 → plan 031** (build): finish the HealthKit re-ask gate per the plan-06/plan-09 spec.

## Findings considered and rejected

- **SEC-05 (dependency audit)**: 1 critical + 32 high advisories all confined to dev/build tooling (esbuild via convex CLI, xmldom via Expo CLI, shell-quote via react-devtools) — no runtime exposure. Track as toolchain hygiene. (Re-audit when a runtime dep appears in `pnpm audit`.)
- **DEBT-07 (913-line settings screen split)**: change-cost only, no behavioral bug; defer until the next settings feature forces a touch. Plan 002 removes the most dangerous logic embedded in it.
- **SEC-04 (AI prompt self-injection)**: user-owned data in the user's own prompt with tools scoped to the same user — robustness, not a vulnerability today. Revisit immediately if shared/coach content ever enters prompts.
- **backlog.md items #1/#3/#7**: already triaged by the maintainer in `backlog.md` — intentionally not re-planned. (#4, the recipes index, is already fixed at `convex/recipes.ts:37-40`; LATER #2 is instrumented by plan 029 rather than implemented.)
- **smoke-test deletion vs rewrite**: rewritten (plan 024) rather than deleted — a deterministic signed-out anchor is worth one tiny flow.
