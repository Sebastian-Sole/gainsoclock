defaults.url=https://de.sentry.io/
defaults.org=sole-innovations
defaults.project=fitbull
auth.token=
# SENTRY_AUTH_TOKEN env var supplies the secret at build time
# (set locally in ~/.zshrc, in Xcode Cloud workflow env vars).
